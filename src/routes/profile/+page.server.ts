// La fonction load représente la source de donnée de la parge 'profile' s'execute uniquement sur le server (.server)
export function load() {
  return {
    firstName : 'Pierre',
    name : 'Panda',
    age : 32,
  }
}
